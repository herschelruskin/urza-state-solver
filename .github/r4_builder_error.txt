info: syncing channel updates for 1.89.0-x86_64-unknown-linux-gnu
info: latest update on 2025-08-07 for version 1.89.0 (29483883e 2025-08-04)
info: downloading 5 components
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:856:
 
         let chrome = card_id_by_name_from_r1("Chrome Dome")?;
         let chrome_profile = cards
-  .get_mut(&chrome)
-  .ok_or_else(|| CatalogError::Invariant("missing R4 Chrome Dome profile".to_owned()))?;
+            .get_mut(&chrome)
+            .ok_or_else(|| CatalogError::Invariant("missing R4 Chrome Dome profile".to_owned()))?;
         chrome_profile.role = urza_rules::R2CardRole::CreaturePermanent;
         chrome_profile.engine = urza_rules::EngineKind::ChromeDome;
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:864:
         let vault = card_id_by_name_from_r1("Mana Vault")?;
         let vault_profile = cards
-  .get_mut(&vault)
-  .ok_or_else(|| CatalogError::Invariant("missing R4 Mana Vault profile".to_owned()))?;
+            .get_mut(&vault)
+            .ok_or_else(|| CatalogError::Invariant("missing R4 Mana Vault profile".to_owned()))?;
         vault_profile.role = urza_rules::R2CardRole::ArtifactPermanent;
         vault_profile.engine = urza_rules::EngineKind::ManaVault;
         vault_profile.mana_ability = urza_rules::ManaAbility::TapForColorless(3);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:871:
         vault_profile.skip_normal_untap = true;
 
         for name in ["Banishing Knack", "Retraction Helix"] {
-  let card = card_id_by_name_from_r1(name)?;
-  let profile = cards
-      .get_mut(&card)
-      .ok_or_else(|| CatalogError::Invariant(format!("missing R4 profile for {name}")))?;
-  profile.role = urza_rules::R2CardRole::TargetedEffectSpell;
-  profile.spell_effect = urza_rules::SpellEffectKind::KnackBounceGrant;
+            let card = card_id_by_name_from_r1(name)?;
+            let profile = cards
+                .get_mut(&card)
+                .ok_or_else(|| CatalogError::Invariant(format!("missing R4 profile for {name}")))?;
+            profile.role = urza_rules::R2CardRole::TargetedEffectSpell;
+            profile.spell_effect = urza_rules::SpellEffectKind::KnackBounceGrant;
         }
 
         let floodcaller = card_id_by_name_from_r1("Valley Floodcaller")?;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:883:
         let floodcaller_profile = cards.get_mut(&floodcaller).ok_or_else(|| {
-  CatalogError::Invariant("missing R4 Valley Floodcaller profile".to_owned())
+            CatalogError::Invariant("missing R4 Valley Floodcaller profile".to_owned())
         })?;
         floodcaller_profile.role = urza_rules::R2CardRole::CreaturePermanent;
         floodcaller_profile.engine = urza_rules::EngineKind::ValleyFloodcaller;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:889:
 
         let cam = card_id_by_name_from_r1("Sewer-veillance Cam")?;
         let cam_profile = cards
-  .get_mut(&cam)
-  .ok_or_else(|| CatalogError::Invariant("missing R4 Cam profile".to_owned()))?;
+            .get_mut(&cam)
+            .ok_or_else(|| CatalogError::Invariant("missing R4 Cam profile".to_owned()))?;
         cam_profile.role = urza_rules::R2CardRole::ArtifactPermanent;
         cam_profile.utility = urza_rules::UtilityKind::SewerVeillanceCam;
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-cards/src/lib.rs:897:
         cards.insert(
-  CLUE_TOKEN_CARD_ID,
-  urza_rules::CardProfile {
-      card: CLUE_TOKEN_CARD_ID,
-      mana_cost: None,
-      mana_value: 0,
-      role: urza_rules::R2CardRole::ArtifactPermanent,
-      battlefield_face: CardFace::Front,
-      is_artifact: true,
-      ..urza_rules::CardProfile::default()
-  },
+            CLUE_TOKEN_CARD_ID,
+            urza_rules::CardProfile {
+                card: CLUE_TOKEN_CARD_ID,
+                mana_cost: None,
+                mana_value: 0,
+                role: urza_rules::R2CardRole::ArtifactPermanent,
+                battlefield_face: CardFace::Front,
+                is_artifact: true,
+                ..urza_rules::CardProfile::default()
+            },
         );
 
         Ok(Self { cards })
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-info/src/lib.rs:322:
                 ability: Some(*ability),
                 parameter: None,
             },
-            StackObject::TargetedControlledTrigger { source, ability, target } => {
-                ObservedStackObject {
-                    kind: ObservedStackKind::TargetedControlledTrigger,
-                    card: None,
-                    source: Some(observe_source(*source, &object_classes)),
-                    target: Some(observe_source(*target, &object_classes)),
-                    ability: Some(*ability),
-                    parameter: None,
-                }
-            }
+            StackObject::TargetedControlledTrigger {
+                source,
+                ability,
+                target,
+            } => ObservedStackObject {
+                kind: ObservedStackKind::TargetedControlledTrigger,
+                card: None,
+                source: Some(observe_source(*source, &object_classes)),
+                target: Some(observe_source(*target, &object_classes)),
+                ability: Some(*ability),
+                parameter: None,
+            },
             StackObject::ActivatedAbility {
                 source,
                 ability,
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:763:
             activate_grinding_station(state, cards, source, sacrifice)?;
             Transition::default()
         }
-        Action::ActivateChromeDome { source, target, payment } => {
+        Action::ActivateChromeDome {
+            source,
+            target,
+            payment,
+        } => {
             activate_chrome_dome(state, cards, source, target, payment)?;
             Transition::default()
         }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:992:
                         .is_some_and(|profile| profile.is_creature)
                 })
                 .filter_map(|permanent| {
-                    seen.insert(permanent.canonical_id).then_some(Action::ChooseCamTarget {
-                        target: permanent.canonical_id,
-                    })
+                    seen.insert(permanent.canonical_id)
+                        .then_some(Action::ChooseCamTarget {
+                            target: permanent.canonical_id,
+                        })
                 })
                 .collect()
         }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1010:
                 .map(|permanent| permanent.tapped);
             match tapped {
                 Some(true) => vec![
-                    Action::ChooseCamEffect { choice: CamEffectChoice::Untap },
-                    Action::ChooseCamEffect { choice: CamEffectChoice::Decline },
+                    Action::ChooseCamEffect {
+                        choice: CamEffectChoice::Untap,
+                    },
+                    Action::ChooseCamEffect {
+                        choice: CamEffectChoice::Decline,
+                    },
                 ],
                 Some(false) => vec![
-                    Action::ChooseCamEffect { choice: CamEffectChoice::Tap },
-                    Action::ChooseCamEffect { choice: CamEffectChoice::Decline },
+                    Action::ChooseCamEffect {
+                        choice: CamEffectChoice::Tap,
+                    },
+                    Action::ChooseCamEffect {
+                        choice: CamEffectChoice::Decline,
+                    },
                 ],
-                None => vec![Action::ChooseCamEffect { choice: CamEffectChoice::Decline }],
+                None => vec![Action::ChooseCamEffect {
+                    choice: CamEffectChoice::Decline,
+                }],
             }
         }
         ObservedPendingDecision::ScryChoice { looked_at, .. } => {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1293:
         let mana_total = public_mana_total(information.mana);
         let ready_station = information.battlefield.iter().any(|permanent| {
             !permanent.tapped
-                && cards.profile(permanent.card).is_some_and(|profile| {
-                    profile.engine == EngineKind::GrindingStation
-                })
+                && cards
+                    .profile(permanent.card)
+                    .is_some_and(|profile| profile.engine == EngineKind::GrindingStation)
         });
         if ready_station && mana_total >= u32::from(effective_cost) {
             return Some(WinFamily::ChromeDomeGrindingStation);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1302:
         }
         let ready_golem = information.battlefield.iter().any(|permanent| {
             !permanent.tapped
-                && cards.profile(permanent.card).is_some_and(|profile| {
-                    profile.engine == EngineKind::BatteredGolem
-                })
+                && cards
+                    .profile(permanent.card)
+                    .is_some_and(|profile| profile.engine == EngineKind::BatteredGolem)
         });
         if ready_golem && mana_total >= u32::from(effective_cost) {
             return Some(WinFamily::ChromeDomeBatteredGolem);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1311:
         }
 
         let gadgeteer = information.battlefield.iter().any(|permanent| {
-            cards.profile(permanent.card).is_some_and(|profile| {
-                profile.engine == EngineKind::ForensicGadgeteer
-            })
+            cards
+                .profile(permanent.card)
+                .is_some_and(|profile| profile.engine == EngineKind::ForensicGadgeteer)
         });
         let ready_vault = information.battlefield.iter().any(|permanent| {
             !permanent.tapped
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1320:
-                && cards.profile(permanent.card).is_some_and(|profile| {
-                    profile.engine == EngineKind::ManaVault
-                })
+                && cards
+                    .profile(permanent.card)
+                    .is_some_and(|profile| profile.engine == EngineKind::ManaVault)
         });
         let power_artifact_attached = information.battlefield.iter().any(|permanent| {
             cards.profile(permanent.card).is_some_and(|profile| {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1343:
         .collect();
     let ready_cam = information.battlefield.iter().any(|permanent| {
         !permanent.tapped
-            && cards.profile(permanent.card).is_some_and(|profile| {
-                profile.utility == UtilityKind::SewerVeillanceCam
-            })
+            && cards
+                .profile(permanent.card)
+                .is_some_and(|profile| profile.utility == UtilityKind::SewerVeillanceCam)
     });
     if ready_cam && !ready_granted.is_empty() {
         return Some(WinFamily::KnackHelixCam);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1360:
         if !source_is_floodcaller && !source_is_golem {
             continue;
         }
-        let replay = information.hand.iter().filter_map(|card| cards.profile(*card)).find(|profile| {
-            profile.is_artifact
-                && (!source_is_floodcaller || !profile.is_creature)
-                && profile.mana_cost.is_some_and(|cost| cost.required_total() <= 1)
-        });
+        let replay = information
+            .hand
+            .iter()
+            .filter_map(|card| cards.profile(*card))
+            .find(|profile| {
+                profile.is_artifact
+                    && (!source_is_floodcaller || !profile.is_creature)
+                    && profile
+                        .mana_cost
+                        .is_some_and(|cost| cost.required_total() <= 1)
+            });
         let Some(replay) = replay else {
             continue;
         };
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1511:
 }
 
 fn public_mana_total(pool: ManaPool) -> u32 {
-    [pool.white, pool.blue, pool.black, pool.red, pool.green, pool.colorless]
-        .into_iter()
-        .map(u32::from)
-        .sum()
+    [
+        pool.white,
+        pool.blue,
+        pool.black,
+        pool.red,
+        pool.green,
+        pool.colorless,
+    ]
+    .into_iter()
+    .map(u32::from)
+    .sum()
 }
 
 fn public_artifact_activation_generic_cost<D: CardDatabase>(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1528:
         if let Some(profile) = cards.profile(permanent.card) {
             reduction = reduction.saturating_add(profile.artifact_activation_reduction);
             if permanent.attached_to == Some(source.canonical_id) {
-                reduction = reduction.saturating_add(profile.attached_artifact_activation_reduction);
+                reduction =
+                    reduction.saturating_add(profile.attached_artifact_activation_reduction);
             }
         }
     }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1773:
     }
     let sacrifice_id = resolve_canonical_object(state, sacrifice)
         .map_err(|error| match error {
-  urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
+            urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
         })?
         .ok_or(RuleError::MissingCanonicalPermanent(sacrifice))?;
     validate_sacrifice_artifact(state, cards, sacrifice_id)?;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1781:
     sacrifice_artifact(state, sacrifice_id)?;
     state.stack.push(StackObject::ActivatedAbility {
         source: SourceRef {
-  object_id: Some(source),
-  card: station.card,
+            object_id: Some(source),
+            card: station.card,
         },
         ability: ABILITY_GRINDING_STATION_MILL,
         parameter: None,
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1806:
     }
     let target_id = resolve_canonical_object(state, target)
         .map_err(|error| match error {
-  urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
+            urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
         })?
         .ok_or(RuleError::MissingCanonicalPermanent(target))?;
     if target_id == source {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1821:
     spend_payment(&mut state.mana, payment);
     state.stack.push(StackObject::TargetedActivatedAbility {
         source: SourceRef {
-  object_id: Some(source),
-  card: dome.card,
+            object_id: Some(source),
+            card: dome.card,
         },
         ability: ABILITY_CHROME_DOME_COPY,
         target: SourceRef {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1829:
-  object_id: Some(target_id),
-  card: target_permanent.card,
+            object_id: Some(target_id),
+            card: target_permanent.card,
         },
         parameter: None,
     });
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1857:
     }
     let target_id = resolve_canonical_object(state, target)
         .map_err(|error| match error {
-  urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
+            urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
         })?
         .ok_or(RuleError::MissingCanonicalPermanent(target))?;
     let target_permanent = battlefield_permanent(state, target_id)?.clone();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1875:
     set_tapped(state, source)?;
     state.stack.push(StackObject::TargetedActivatedAbility {
         source: SourceRef {
-  object_id: Some(source),
-  card: creature.card,
+            object_id: Some(source),
+            card: creature.card,
         },
         ability: ABILITY_KNACK_BOUNCE,
         target: SourceRef {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:1883:
-  object_id: Some(target_id),
-  card: target_permanent.card,
+            object_id: Some(target_id),
+            card: target_permanent.card,
         },
         parameter: None,
     });
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:2686:
     }
     let target_id = resolve_canonical_object(state, target)
         .map_err(|error| match error {
-  urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
+            urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
         })?
         .ok_or(RuleError::MissingCanonicalPermanent(target))?;
     let target_permanent = battlefield_permanent(state, target_id)?.clone();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:2693:
     if !permanent_is_creature(cards, &target_permanent)? {
         return Err(RuleError::InvalidPermanentTarget);
     }
-    let cost = profile.mana_cost.ok_or(RuleError::UnsupportedCardMechanic(card))?;
+    let cost = profile
+        .mana_cost
+        .ok_or(RuleError::UnsupportedCardMechanic(card))?;
     validate_payment(state.mana, payment, cost)?;
     let object_id = next_object_id(state)?;
     spend_payment(&mut state.mana, payment);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:2703:
         object_id,
         card,
         target: SourceRef {
-  object_id: Some(target_id),
-  card: target_permanent.card,
+            object_id: Some(target_id),
+            card: target_permanent.card,
         },
     });
     queue_cast_triggers(state, cards, card);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3112:
             )
         }
         StackObject::ActivatedAbility {
-  ability: ABILITY_GRINDING_STATION_MILL,
-  ..
+            ability: ABILITY_GRINDING_STATION_MILL,
+            ..
         } => {
-  state.stack.pop();
-  state.window = Window::Priority;
-  Ok(Transition {
-      observations: vec![RulesObservation::OpponentMilled { cards: 3 }],
-  })
+            state.stack.pop();
+            state.window = Window::Priority;
+            Ok(Transition {
+                observations: vec![RulesObservation::OpponentMilled { cards: 3 }],
+            })
         }
         StackObject::ControlledTrigger {
-  source,
-  ability: ABILITY_ARTIFACT_ENTRY_UNTAP,
+            source,
+            ability: ABILITY_ARTIFACT_ENTRY_UNTAP,
         } => {
-  state.stack.pop();
-  resolve_artifact_entry_untap(state, source)
+            state.stack.pop();
+            resolve_artifact_entry_untap(state, source)
         }
         StackObject::ControlledTrigger {
-  source,
-  ability: ABILITY_GADGETEER_INVESTIGATE,
+            source,
+            ability: ABILITY_GADGETEER_INVESTIGATE,
         } => {
-  state.stack.pop();
-  resolve_gadgeteer_investigate(state, cards, source)
+            state.stack.pop();
+            resolve_gadgeteer_investigate(state, cards, source)
         }
         StackObject::ControlledTrigger {
-  source,
-  ability: ABILITY_FLOODCALLER_UNTAP,
+            source,
+            ability: ABILITY_FLOODCALLER_UNTAP,
         } => {
-  state.stack.pop();
-  resolve_floodcaller_untap(state, cards, source)
+            state.stack.pop();
+            resolve_floodcaller_untap(state, cards, source)
         }
         StackObject::ControlledTrigger {
-  source,
-  ability: ABILITY_CAM_TAP_UNTAP,
+            source,
+            ability: ABILITY_CAM_TAP_UNTAP,
         } => {
-  state.stack.pop();
-  stage_cam_target(state, cards, source)
+            state.stack.pop();
+            stage_cam_target(state, cards, source)
         }
         StackObject::TargetedControlledTrigger {
-  source,
-  ability: ABILITY_CAM_TAP_UNTAP,
-  target,
+            source,
+            ability: ABILITY_CAM_TAP_UNTAP,
+            target,
         } => {
-  state.stack.pop();
-  stage_cam_effect(state, source, target)
+            state.stack.pop();
+            stage_cam_effect(state, source, target)
         }
         StackObject::TargetedActivatedAbility {
-  source,
-  ability: ABILITY_CHROME_DOME_COPY,
-  target,
-  ..
+            source,
+            ability: ABILITY_CHROME_DOME_COPY,
+            target,
+            ..
         } => {
-  state.stack.pop();
-  resolve_chrome_dome_copy(state, cards, source, target)
+            state.stack.pop();
+            resolve_chrome_dome_copy(state, cards, source, target)
         }
         StackObject::ControlledTrigger {
-  source,
-  ability: ABILITY_CHROME_DOME_SACRIFICE,
+            source,
+            ability: ABILITY_CHROME_DOME_SACRIFICE,
         } => {
-  state.stack.pop();
-  resolve_chrome_dome_sacrifice(state, cards, source)
+            state.stack.pop();
+            resolve_chrome_dome_sacrifice(state, cards, source)
         }
         StackObject::TargetedActivatedAbility {
-  ability: ABILITY_KNACK_BOUNCE,
-  target,
-  ..
+            ability: ABILITY_KNACK_BOUNCE,
+            target,
+            ..
         } => {
-  state.stack.pop();
-  resolve_knack_bounce(state, cards, target)
+            state.stack.pop();
+            resolve_knack_bounce(state, cards, target)
         }
         StackObject::ControlledTrigger { .. }
         | StackObject::TargetedControlledTrigger { .. }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3320:
     state.graveyard.insert(card);
     if let Some(target_id) = target.object_id
         && state
-  .battlefield
-  .get(target_id)
-  .is_some_and(|permanent| permanent.card == target.card)
+            .battlefield
+            .get(target_id)
+            .is_some_and(|permanent| permanent.card == target.card)
     {
         let mut permanents = state.battlefield.permanents().to_vec();
         if let Some(permanent) = permanents
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3329:
-  .iter_mut()
-  .find(|permanent| permanent.object_id == target_id)
+            .iter_mut()
+            .find(|permanent| permanent.object_id == target_id)
         {
-  permanent.granted_ability = Some(urza_core::GrantedAbility::KnackBounceUntilEndOfTurn);
+            permanent.granted_ability = Some(urza_core::GrantedAbility::KnackBounceUntilEndOfTurn);
         }
         state.battlefield = BattlefieldZone::new(permanents);
     }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3343:
 ) -> Result<Transition, RuleError> {
     let live_tapped = source.object_id.is_some_and(|object_id| {
         state
-  .battlefield
-  .get(object_id)
-  .is_some_and(|permanent| permanent.card == source.card && permanent.tapped)
+            .battlefield
+            .get(object_id)
+            .is_some_and(|permanent| permanent.card == source.card && permanent.tapped)
     });
     if live_tapped {
         state.pending = PendingDecision::ProducerUntapChoice { source };
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3366:
     if untap
         && let Some(object_id) = source.object_id
         && state
-  .battlefield
-  .get(object_id)
-  .is_some_and(|permanent| permanent.card == source.card)
+            .battlefield
+            .get(object_id)
+            .is_some_and(|permanent| permanent.card == source.card)
     {
         set_untapped(state, object_id)?;
     }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3382:
     cards: &D,
     _source: SourceRef,
 ) -> Result<Transition, RuleError> {
-    let clue = cards.clue_token_card().ok_or(RuleError::UnsupportedStackObject)?;
+    let clue = cards
+        .clue_token_card()
+        .ok_or(RuleError::UnsupportedStackObject)?;
     let profile = card_profile(cards, clue)?;
     let object_id = next_object_id(state)?;
     insert_permanent(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3389:
         state,
         cards,
         PermanentState {
-  object_id,
-  card: clue,
-  face: profile.battlefield_face,
-  tapped: false,
-  summoning_sick: false,
-  token: true,
-  counters: CounterState::default(),
-  mode: PermanentMode::Normal,
-  attached_to: None,
-  granted_ability: None,
+            object_id,
+            card: clue,
+            face: profile.battlefield_face,
+            tapped: false,
+            summoning_sick: false,
+            token: true,
+            counters: CounterState::default(),
+            mode: PermanentMode::Normal,
+            attached_to: None,
+            granted_ability: None,
         },
     );
     state.window = Window::Priority;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3405:
     Ok(Transition {
         observations: vec![RulesObservation::PermanentEntered {
-  card: clue,
-  face: profile.battlefield_face,
-  token: true,
+            card: clue,
+            face: profile.battlefield_face,
+            token: true,
         }],
     })
 }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3419:
     let mut permanents = state.battlefield.permanents().to_vec();
     for permanent in &mut permanents {
         if cards
-  .profile(permanent.card)
-  .is_some_and(|profile| profile.floodcaller_untap_eligible)
+            .profile(permanent.card)
+            .is_some_and(|profile| profile.floodcaller_untap_eligible)
         {
-  permanent.tapped = false;
+            permanent.tapped = false;
         }
     }
     state.battlefield = BattlefieldZone::new(permanents);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3437:
 ) -> Result<Transition, RuleError> {
     let has_creature = state.battlefield.permanents().iter().any(|permanent| {
         cards
-  .profile(permanent.card)
-  .is_some_and(|profile| profile.is_creature)
+            .profile(permanent.card)
+            .is_some_and(|profile| profile.is_creature)
     });
     if has_creature {
         state.pending = PendingDecision::CamTarget { source };
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3462:
     };
     let target_id = resolve_canonical_object(state, target)
         .map_err(|error| match error {
-  urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
+            urza_info::ObservationError::InvalidState(error) => RuleError::InvalidState(error),
         })?
         .ok_or(RuleError::MissingCanonicalPermanent(target))?;
     let permanent = battlefield_permanent(state, target_id)?.clone();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3474:
         source,
         ability: ABILITY_CAM_TAP_UNTAP,
         target: SourceRef {
-  object_id: Some(target_id),
-  card: permanent.card,
+            object_id: Some(target_id),
+            card: permanent.card,
         },
     });
     state.window = Window::Priority;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3504:
     };
     if let Some(object_id) = target.object_id
         && state
-  .battlefield
-  .get(object_id)
-  .is_some_and(|permanent| permanent.card == target.card)
+            .battlefield
+            .get(object_id)
+            .is_some_and(|permanent| permanent.card == target.card)
     {
         match choice {
-  CamEffectChoice::Decline => {}
-  CamEffectChoice::Tap => set_tapped(state, object_id)?,
-  CamEffectChoice::Untap => set_untapped(state, object_id)?,
+            CamEffectChoice::Decline => {}
+            CamEffectChoice::Tap => set_tapped(state, object_id)?,
+            CamEffectChoice::Untap => set_untapped(state, object_id)?,
         }
     }
     state.pending = PendingDecision::None;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3543:
         state,
         cards,
         PermanentState {
-  object_id,
-  card: target.card,
-  face: profile.battlefield_face,
-  tapped: false,
-  summoning_sick: profile.is_creature,
-  token: true,
-  counters: CounterState {
-      loyalty: profile.starting_loyalty,
-      ..CounterState::default()
-  },
-  mode: initial_permanent_mode(profile),
-  attached_to: None,
-  granted_ability: None,
+            object_id,
+            card: target.card,
+            face: profile.battlefield_face,
+            tapped: false,
+            summoning_sick: profile.is_creature,
+            token: true,
+            counters: CounterState {
+                loyalty: profile.starting_loyalty,
+                ..CounterState::default()
+            },
+            mode: initial_permanent_mode(profile),
+            attached_to: None,
+            granted_ability: None,
         },
     );
     let due_turn = if state.phase == Phase::EndStep {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3562:
-        state.turn.checked_add(1).ok_or(RuleError::ArithmeticOverflow)?
+        state
+            .turn
+            .checked_add(1)
+            .ok_or(RuleError::ArithmeticOverflow)?
     } else {
         state.turn
     };
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3566:
-    state.delayed_events.push(DelayedEvent::ChromeCopySacrifice {
-        object: object_id,
-        card: target.card,
-        due_turn,
-    });
+    state
+        .delayed_events
+        .push(DelayedEvent::ChromeCopySacrifice {
+            object: object_id,
+            card: target.card,
+            due_turn,
+        });
     state.window = Window::Priority;
     Ok(Transition {
         observations: vec![RulesObservation::PermanentEntered {
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3574:
-  card: target.card,
-  face: profile.battlefield_face,
-  token: true,
+            card: target.card,
+            face: profile.battlefield_face,
+            token: true,
         }],
     })
 }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:3585:
 ) -> Result<Transition, RuleError> {
     if let Some(object_id) = source.object_id
         && state
-  .battlefield
-  .get(object_id)
-  .is_some_and(|permanent| permanent.card == source.card)
+            .battlefield
+            .get(object_id)
+            .is_some_and(|permanent| permanent.card == source.card)
     {
         let was_cam = cards
-  .profile(source.card)
-  .is_some_and(|profile| profile.utility == UtilityKind::SewerVeillanceCam);
+            .profile(source.card)
+            .is_some_and(|profile| profile.utility == UtilityKind::SewerVeillanceCam);
         sacrifice_artifact(state, object_id)?;
         if was_cam {
-  queue_cam_leave_trigger(state, source.card);
+            queue_cam_leave_trigger(state, source.card);
         }
     }
     state.window = Window::Priority;
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4879:
     Ok(())
 }
 
-fn insert_permanent<D: CardDatabase>(
-    state: &mut TrueState,
-    cards: &D,
-    permanent: PermanentState,
-) {
+fn insert_permanent<D: CardDatabase>(state: &mut TrueState, cards: &D, permanent: PermanentState) {
     let entered = permanent.clone();
     let mut permanents = state.battlefield.permanents().to_vec();
     permanents.push(permanent);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4895:
     if entered_profile.is_artifact {
         let mut triggers = Vec::new();
         for producer in state.battlefield.permanents() {
-  let Some(profile) = cards.profile(producer.card) else {
-      continue;
-  };
-  if matches!(profile.engine, EngineKind::GrindingStation | EngineKind::BatteredGolem) {
-      triggers.push(StackObject::ControlledTrigger {
-          source: SourceRef {
-              object_id: Some(producer.object_id),
-              card: producer.card,
-          },
-          ability: ABILITY_ARTIFACT_ENTRY_UNTAP,
-      });
-  }
+            let Some(profile) = cards.profile(producer.card) else {
+                continue;
+            };
+            if matches!(
+                profile.engine,
+                EngineKind::GrindingStation | EngineKind::BatteredGolem
+            ) {
+                triggers.push(StackObject::ControlledTrigger {
+                    source: SourceRef {
+                        object_id: Some(producer.object_id),
+                        card: producer.card,
+                    },
+                    ability: ABILITY_ARTIFACT_ENTRY_UNTAP,
+                });
+            }
         }
         state.stack.extend(triggers);
     }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4913:
     if entered_profile.utility == UtilityKind::SewerVeillanceCam {
         state.stack.push(StackObject::ControlledTrigger {
-  source: SourceRef {
-      object_id: Some(entered.object_id),
-      card: entered.card,
-  },
-  ability: ABILITY_CAM_TAP_UNTAP,
+            source: SourceRef {
+                object_id: Some(entered.object_id),
+                card: entered.card,
+            },
+            ability: ABILITY_CAM_TAP_UNTAP,
         });
     }
 }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4928:
     let mut triggers = Vec::new();
     for permanent in state.battlefield.permanents() {
         let Some(profile) = cards.profile(permanent.card) else {
-  continue;
+            continue;
         };
         if cast_profile.is_artifact && profile.engine == EngineKind::ForensicGadgeteer {
-  triggers.push(StackObject::ControlledTrigger {
-      source: SourceRef {
-          object_id: Some(permanent.object_id),
-          card: permanent.card,
-      },
-      ability: ABILITY_GADGETEER_INVESTIGATE,
-  });
+            triggers.push(StackObject::ControlledTrigger {
+                source: SourceRef {
+                    object_id: Some(permanent.object_id),
+                    card: permanent.card,
+                },
+                ability: ABILITY_GADGETEER_INVESTIGATE,
+            });
         }
         if !cast_profile.is_creature && profile.engine == EngineKind::ValleyFloodcaller {
-  triggers.push(StackObject::ControlledTrigger {
-      source: SourceRef {
-          object_id: Some(permanent.object_id),
-          card: permanent.card,
-      },
-      ability: ABILITY_FLOODCALLER_UNTAP,
-  });
+            triggers.push(StackObject::ControlledTrigger {
+                source: SourceRef {
+                    object_id: Some(permanent.object_id),
+                    card: permanent.card,
+                },
+                ability: ABILITY_FLOODCALLER_UNTAP,
+            });
         }
     }
     state.stack.extend(triggers);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4955:
 fn queue_cam_leave_trigger(state: &mut TrueState, card: CardDefId) {
     state.stack.push(StackObject::ControlledTrigger {
         source: SourceRef {
-  object_id: None,
-  card,
+            object_id: None,
+            card,
         },
         ability: ABILITY_CAM_TAP_UNTAP,
     });
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4966:
     let mut due = Vec::new();
     state.delayed_events.retain(|event| match event {
         DelayedEvent::ChromeCopySacrifice {
-  object,
-  card,
-  due_turn,
+            object,
+            card,
+            due_turn,
         } if *due_turn <= state.turn => {
-  due.push((*object, *card));
-  false
+            due.push((*object, *card));
+            false
         }
         _ => true,
     });
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4978:
     for (object, card) in due {
         state.stack.push(StackObject::ControlledTrigger {
-  source: SourceRef {
-      object_id: Some(object),
-      card,
-  },
-  ability: ABILITY_CHROME_DOME_SACRIFICE,
+            source: SourceRef {
+                object_id: Some(object),
+                card,
+            },
+            ability: ABILITY_CHROME_DOME_SACRIFICE,
         });
     }
 }
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4990:
     let mut permanents = state.battlefield.permanents().to_vec();
     for permanent in &mut permanents {
         if permanent.granted_ability == Some(urza_core::GrantedAbility::KnackBounceUntilEndOfTurn) {
-  permanent.granted_ability = None;
+            permanent.granted_ability = None;
         }
     }
     state.battlefield = BattlefieldZone::new(permanents);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:4997:
 }
 
-
 fn next_object_id(state: &TrueState) -> Result<ObjectId, RuleError> {
     let battlefield_max = state
         .battlefield
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:5548:
                 },
             );
             cards.profiles.insert(
-      CHROME,
-      CardProfile {
-          card: CHROME,
-          mana_cost: Some(ManaCost {
-              generic: 2,
-              ..ManaCost::default()
-          }),
-          mana_value: 2,
-          role: R2CardRole::CreaturePermanent,
-          battlefield_face: CardFace::Front,
-          engine: EngineKind::ChromeDome,
-          is_artifact: true,
-          is_creature: true,
-          ..CardProfile::default()
-      },
-  );
-  cards.profiles.insert(
-      VAULT,
-      CardProfile {
-          card: VAULT,
-          mana_cost: Some(ManaCost {
-              generic: 1,
-              ..ManaCost::default()
-          }),
-          mana_value: 1,
-          role: R2CardRole::ArtifactPermanent,
-          battlefield_face: CardFace::Front,
-          mana_ability: ManaAbility::TapForColorless(3),
-          engine: EngineKind::ManaVault,
-          skip_normal_untap: true,
-          is_artifact: true,
-          ..CardProfile::default()
-      },
-  );
-  for card in [KNACK, HELIX] {
-      cards.profiles.insert(
-          card,
-          CardProfile {
-              card,
-              mana_cost: Some(ManaCost {
-                  blue: 1,
-                  ..ManaCost::default()
-              }),
-              mana_value: 1,
-              role: R2CardRole::TargetedEffectSpell,
-              spell_effect: SpellEffectKind::KnackBounceGrant,
-              ..CardProfile::default()
-          },
-      );
-  }
-  cards.profiles.insert(
-      FLOODCALLER,
-      CardProfile {
-          card: FLOODCALLER,
-          mana_cost: Some(ManaCost {
-              blue: 1,
-              generic: 2,
-              ..ManaCost::default()
-          }),
-          mana_value: 3,
-          role: R2CardRole::CreaturePermanent,
-          battlefield_face: CardFace::Front,
-          engine: EngineKind::ValleyFloodcaller,
-          floodcaller_untap_eligible: true,
-          is_creature: true,
-          ..CardProfile::default()
-      },
-  );
-  cards.profiles.insert(
-      CAM,
-      CardProfile {
-          card: CAM,
-          mana_cost: Some(ManaCost {
-              blue: 1,
-              ..ManaCost::default()
-          }),
-          mana_value: 1,
-          role: R2CardRole::ArtifactPermanent,
-          battlefield_face: CardFace::Front,
-          utility: UtilityKind::SewerVeillanceCam,
-          is_artifact: true,
-          ..CardProfile::default()
-      },
-  );
-  cards.profiles.insert(
-      CLUE,
-      CardProfile {
-          card: CLUE,
-          role: R2CardRole::ArtifactPermanent,
-          battlefield_face: CardFace::Front,
-          is_artifact: true,
-          ..CardProfile::default()
-      },
-  );
-  cards
+                CHROME,
+                CardProfile {
+                    card: CHROME,
+                    mana_cost: Some(ManaCost {
+                        generic: 2,
+                        ..ManaCost::default()
+                    }),
+                    mana_value: 2,
+                    role: R2CardRole::CreaturePermanent,
+                    battlefield_face: CardFace::Front,
+                    engine: EngineKind::ChromeDome,
+                    is_artifact: true,
+                    is_creature: true,
+                    ..CardProfile::default()
+                },
+            );
+            cards.profiles.insert(
+                VAULT,
+                CardProfile {
+                    card: VAULT,
+                    mana_cost: Some(ManaCost {
+                        generic: 1,
+                        ..ManaCost::default()
+                    }),
+                    mana_value: 1,
+                    role: R2CardRole::ArtifactPermanent,
+                    battlefield_face: CardFace::Front,
+                    mana_ability: ManaAbility::TapForColorless(3),
+                    engine: EngineKind::ManaVault,
+                    skip_normal_untap: true,
+                    is_artifact: true,
+                    ..CardProfile::default()
+                },
+            );
+            for card in [KNACK, HELIX] {
+                cards.profiles.insert(
+                    card,
+                    CardProfile {
+                        card,
+                        mana_cost: Some(ManaCost {
+                            blue: 1,
+                            ..ManaCost::default()
+                        }),
+                        mana_value: 1,
+                        role: R2CardRole::TargetedEffectSpell,
+                        spell_effect: SpellEffectKind::KnackBounceGrant,
+                        ..CardProfile::default()
+                    },
+                );
+            }
+            cards.profiles.insert(
+                FLOODCALLER,
+                CardProfile {
+                    card: FLOODCALLER,
+                    mana_cost: Some(ManaCost {
+                        blue: 1,
+                        generic: 2,
+                        ..ManaCost::default()
+                    }),
+                    mana_value: 3,
+                    role: R2CardRole::CreaturePermanent,
+                    battlefield_face: CardFace::Front,
+                    engine: EngineKind::ValleyFloodcaller,
+                    floodcaller_untap_eligible: true,
+                    is_creature: true,
+                    ..CardProfile::default()
+                },
+            );
+            cards.profiles.insert(
+                CAM,
+                CardProfile {
+                    card: CAM,
+                    mana_cost: Some(ManaCost {
+                        blue: 1,
+                        ..ManaCost::default()
+                    }),
+                    mana_value: 1,
+                    role: R2CardRole::ArtifactPermanent,
+                    battlefield_face: CardFace::Front,
+                    utility: UtilityKind::SewerVeillanceCam,
+                    is_artifact: true,
+                    ..CardProfile::default()
+                },
+            );
+            cards.profiles.insert(
+                CLUE,
+                CardProfile {
+                    card: CLUE,
+                    role: R2CardRole::ArtifactPermanent,
+                    battlefield_face: CardFace::Front,
+                    is_artifact: true,
+                    ..CardProfile::default()
+                },
+            );
+            cards
         }
     }
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8035:
 
     fn test_perm(object: u32, card: CardDefId, tapped: bool) -> PermanentState {
         PermanentState {
-  object_id: ObjectId(object),
-  card,
-  face: CardFace::Front,
-  tapped,
-  summoning_sick: false,
-  token: false,
-  counters: CounterState::default(),
-  mode: PermanentMode::Normal,
-  attached_to: None,
-  granted_ability: None,
+            object_id: ObjectId(object),
+            card,
+            face: CardFace::Front,
+            tapped,
+            summoning_sick: false,
+            token: false,
+            counters: CounterState::default(),
+            mode: PermanentMode::Normal,
+            attached_to: None,
+            granted_ability: None,
         }
     }
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8052:
     fn r4_artifact_entry_and_gadgeteer_cast_triggers_execute_on_stack() {
         let cards = TestCards::r4();
         let mut state = TrueState {
-  turn: 2,
-  phase: Phase::PrecombatMain,
-  window: Window::Priority,
-  battlefield: BattlefieldZone::new(vec![
-      test_perm(1, STATION, true),
-      test_perm(2, GOLEM, true),
-      test_perm(3, GADGETEER, false),
-  ]),
-  ..TrueState::default()
+            turn: 2,
+            phase: Phase::PrecombatMain,
+            window: Window::Priority,
+            battlefield: BattlefieldZone::new(vec![
+                test_perm(1, STATION, true),
+                test_perm(2, GOLEM, true),
+                test_perm(3, GADGETEER, false),
+            ]),
+            ..TrueState::default()
         };
         insert_permanent(
-  &mut state,
-  &cards,
-  PermanentState {
-      object_id: ObjectId(4),
-      card: ARTIFACT_MV0,
-      face: CardFace::Front,
-      tapped: false,
-      summoning_sick: false,
-      token: false,
-      counters: CounterState::default(),
-      mode: PermanentMode::Normal,
-      attached_to: None,
-      granted_ability: None,
-  },
+            &mut state,
+            &cards,
+            PermanentState {
+                object_id: ObjectId(4),
+                card: ARTIFACT_MV0,
+                face: CardFace::Front,
+                tapped: false,
+                summoning_sick: false,
+                token: false,
+                counters: CounterState::default(),
+                mode: PermanentMode::Normal,
+                attached_to: None,
+                granted_ability: None,
+            },
         );
         assert_eq!(state.stack.len(), 2);
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8083:
         let info = urza_info::observe(&state).unwrap();
         assert_eq!(
-  legal_contingent_actions(&info, &cards),
-  vec![
-      Action::ChooseProducerUntap { untap: true },
-      Action::ChooseProducerUntap { untap: false },
-  ]
+            legal_contingent_actions(&info, &cards),
+            vec![
+                Action::ChooseProducerUntap { untap: true },
+                Action::ChooseProducerUntap { untap: false },
+            ]
         );
         apply_action(
-  &mut state,
-  &cards,
-  Action::ChooseProducerUntap { untap: true },
+            &mut state,
+            &cards,
+            Action::ChooseProducerUntap { untap: true },
         )
         .unwrap();
-        assert!(state.battlefield.permanents().iter().any(|p| {
-  matches!(p.card, STATION | GOLEM) && !p.tapped
-        }));
+        assert!(
+            state
+                .battlefield
+                .permanents()
+                .iter()
+                .any(|p| { matches!(p.card, STATION | GOLEM) && !p.tapped })
+        );
 
         let mut cast_state = TrueState {
-  turn: 2,
-  phase: Phase::PrecombatMain,
-  window: Window::Priority,
-  hand: CardZone::new(vec![ARTIFACT_MV0]),
-  battlefield: BattlefieldZone::new(vec![test_perm(1, GADGETEER, false)]),
-  ..TrueState::default()
+            turn: 2,
+            phase: Phase::PrecombatMain,
+            window: Window::Priority,
+            hand: CardZone::new(vec![ARTIFACT_MV0]),
+            battlefield: BattlefieldZone::new(vec![test_perm(1, GADGETEER, false)]),
+            ..TrueState::default()
         };
         apply_action(
-  &mut cast_state,
-  &cards,
-  Action::CastFromHand {
-      card: ARTIFACT_MV0,
-      payment: ManaPayment::default(),
-  },
+            &mut cast_state,
+            &cards,
+            Action::CastFromHand {
+                card: ARTIFACT_MV0,
+                payment: ManaPayment::default(),
+            },
         )
         .unwrap();
         assert!(matches!(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8119:
-  cast_state.stack.last(),
-  Some(StackObject::ControlledTrigger { ability: ABILITY_GADGETEER_INVESTIGATE, .. })
+            cast_state.stack.last(),
+            Some(StackObject::ControlledTrigger {
+                ability: ABILITY_GADGETEER_INVESTIGATE,
+                ..
+            })
         ));
         let transition = apply_action(&mut cast_state, &cards, Action::PassPriority).unwrap();
         assert_eq!(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8124:
-  transition.observations,
-  vec![RulesObservation::PermanentEntered {
-      card: CLUE,
-      face: CardFace::Front,
-      token: true,
-  }]
+            transition.observations,
+            vec![RulesObservation::PermanentEntered {
+                card: CLUE,
+                face: CardFace::Front,
+                token: true,
+            }]
         );
     }
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8134:
     fn r4_grinding_station_activation_commits_cost_and_mills_goldfish_opponent() {
         let cards = TestCards::r4();
         let state_battlefield = BattlefieldZone::new(vec![
-  test_perm(1, STATION, false),
-  test_perm(2, ARTIFACT_MV0, false),
+            test_perm(1, STATION, false),
+            test_perm(2, ARTIFACT_MV0, false),
         ]);
         let mut state = TrueState {
-  turn: 2,
-  phase: Phase::PrecombatMain,
-  window: Window::Priority,
-  battlefield: state_battlefield,
-  ..TrueState::default()
+            turn: 2,
+            phase: Phase::PrecombatMain,
+            window: Window::Priority,
+            battlefield: state_battlefield,
+            ..TrueState::default()
         };
         let info = urza_info::observe(&state).unwrap();
         let sacrifice = info
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8149:
-  .battlefield
-  .iter()
-  .find(|permanent| permanent.card == ARTIFACT_MV0)
-  .unwrap()
-  .canonical_id;
+            .battlefield
+            .iter()
+            .find(|permanent| permanent.card == ARTIFACT_MV0)
+            .unwrap()
+            .canonical_id;
         apply_action(
-  &mut state,
-  &cards,
-  Action::ActivateGrindingStation {
-      source: ObjectId(1),
-      sacrifice,
-  },
+            &mut state,
+            &cards,
+            Action::ActivateGrindingStation {
+                source: ObjectId(1),
+                sacrifice,
+            },
         )
         .unwrap();
         assert!(state.graveyard.cards().contains(&ARTIFACT_MV0));
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8164:
         assert!(state.battlefield.get(ObjectId(1)).unwrap().tapped);
         let resolved = apply_action(&mut state, &cards, Action::PassPriority).unwrap();
         assert_eq!(
-  resolved.observations,
-  vec![RulesObservation::OpponentMilled { cards: 3 }]
+            resolved.observations,
+            vec![RulesObservation::OpponentMilled { cards: 3 }]
         );
     }
 
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8175:
         let mut pa = test_perm(4, POWER_ARTIFACT, false);
         pa.attached_to = Some(ObjectId(1));
         let mut state = TrueState {
-  turn: 2,
-  phase: Phase::PrecombatMain,
-  window: Window::Priority,
-  battlefield: BattlefieldZone::new(vec![
-      test_perm(1, CHROME, false),
-      test_perm(2, VAULT, false),
-      test_perm(3, GADGETEER, false),
-      pa,
-  ]),
-  ..TrueState::default()
+            turn: 2,
+            phase: Phase::PrecombatMain,
+            window: Window::Priority,
+            battlefield: BattlefieldZone::new(vec![
+                test_perm(1, CHROME, false),
+                test_perm(2, VAULT, false),
+                test_perm(3, GADGETEER, false),
+                pa,
+            ]),
+            ..TrueState::default()
         };
         apply_action(
-  &mut state,
-  &cards,
-  Action::ActivateManaAbility { source: ObjectId(2) },
+            &mut state,
+            &cards,
+            Action::ActivateManaAbility {
+                source: ObjectId(2),
+            },
         )
         .unwrap();
         let info = urza_info::observe(&state).unwrap();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8196:
         let target = info
-  .battlefield
-  .iter()
-  .find(|permanent| permanent.card == VAULT)
-  .unwrap()
-  .canonical_id;
+            .battlefield
+            .iter()
+            .find(|permanent| permanent.card == VAULT)
+            .unwrap()
+            .canonical_id;
         apply_action(
-  &mut state,
-  &cards,
-  Action::ActivateChromeDome {
-      source: ObjectId(1),
-      target,
-      payment: ManaPayment {
-          colorless: 2,
-          ..ManaPayment::default()
-      },
-  },
+            &mut state,
+            &cards,
+            Action::ActivateChromeDome {
+                source: ObjectId(1),
+                target,
+                payment: ManaPayment {
+                    colorless: 2,
+                    ..ManaPayment::default()
+                },
+            },
         )
         .unwrap();
         let transition = apply_action(&mut state, &cards, Action::PassPriority).unwrap();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8216:
         assert_eq!(
-  transition.observations,
-  vec![RulesObservation::PermanentEntered {
-      card: VAULT,
-      face: CardFace::Front,
-      token: true,
-  }]
+            transition.observations,
+            vec![RulesObservation::PermanentEntered {
+                card: VAULT,
+                face: CardFace::Front,
+                token: true,
+            }]
         );
         let copy = state
-  .battlefield
-  .permanents()
-  .iter()
-  .find(|permanent| permanent.card == VAULT && permanent.token)
-  .unwrap()
-  .object_id;
+            .battlefield
+            .permanents()
+            .iter()
+            .find(|permanent| permanent.card == VAULT && permanent.token)
+            .unwrap()
+            .object_id;
         assert!(state.delayed_events.iter().any(|event| matches!(
-  event,
-  DelayedEvent::ChromeCopySacrifice { object, due_turn: 2, .. } if *object == copy
-        )));
+        event,
+        DelayedEvent::ChromeCopySacrifice { object, due_turn: 2, .. } if *object == copy
+              )));
         apply_action(
-  &mut state,
-  &cards,
-  Action::ActivateManaAbility { source: copy },
+            &mut state,
+            &cards,
+            Action::ActivateManaAbility { source: copy },
         )
         .unwrap();
         assert_eq!(state.mana.colorless, 4);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8245:
     fn r4_knack_floodcaller_and_cam_recurrence_execution_preserves_exact_grant() {
         let cards = TestCards::r4();
         let mut state = TrueState {
-  turn: 2,
-  phase: Phase::PrecombatMain,
-  window: Window::Priority,
-  hand: CardZone::new(vec![KNACK, ARTIFACT_MV0]),
-  mana: ManaPool {
-      blue: 1,
-      ..ManaPool::default()
-  },
-  battlefield: BattlefieldZone::new(vec![test_perm(1, FLOODCALLER, false)]),
-  ..TrueState::default()
+            turn: 2,
+            phase: Phase::PrecombatMain,
+            window: Window::Priority,
+            hand: CardZone::new(vec![KNACK, ARTIFACT_MV0]),
+            mana: ManaPool {
+                blue: 1,
+                ..ManaPool::default()
+            },
+            battlefield: BattlefieldZone::new(vec![test_perm(1, FLOODCALLER, false)]),
+            ..TrueState::default()
         };
-        let target = urza_info::observe(&state)
-  .unwrap()
-  .battlefield[0]
-  .canonical_id;
+        let target = urza_info::observe(&state).unwrap().battlefield[0].canonical_id;
         apply_action(
-  &mut state,
-  &cards,
-  Action::CastTargetedFromHand {
-      card: KNACK,
-      target,
-      payment: ManaPayment {
-          blue: 1,
-          ..ManaPayment::default()
-      },
-  },
+            &mut state,
+            &cards,
+            Action::CastTargetedFromHand {
+                card: KNACK,
+                target,
+                payment: ManaPayment {
+                    blue: 1,
+                    ..ManaPayment::default()
+                },
+            },
         )
         .unwrap();
         // Floodcaller trigger sits above Knack; resolve it, then the spell.
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8277:
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
         assert_eq!(
-  state.battlefield.get(ObjectId(1)).unwrap().granted_ability,
-  Some(urza_core::GrantedAbility::KnackBounceUntilEndOfTurn)
+            state.battlefield.get(ObjectId(1)).unwrap().granted_ability,
+            Some(urza_core::GrantedAbility::KnackBounceUntilEndOfTurn)
         );
 
         // Cam LTB from the granted bounce untaps the exact granted creature.
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8285:
         insert_permanent(
-  &mut state,
-  &cards,
-  PermanentState {
-      object_id: ObjectId(2),
-      card: CAM,
-      face: CardFace::Front,
-      tapped: false,
-      summoning_sick: false,
-      token: false,
-      counters: CounterState::default(),
-      mode: PermanentMode::Normal,
-      attached_to: None,
-      granted_ability: None,
-  },
+            &mut state,
+            &cards,
+            PermanentState {
+                object_id: ObjectId(2),
+                card: CAM,
+                face: CardFace::Front,
+                tapped: false,
+                summoning_sick: false,
+                token: false,
+                counters: CounterState::default(),
+                mode: PermanentMode::Normal,
+                attached_to: None,
+                granted_ability: None,
+            },
         );
         // Resolve Cam ETB without changing Floodcaller.
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8303:
-        let cam_target = legal_contingent_actions(&urza_info::observe(&state).unwrap(), &cards)[0].clone();
+        let cam_target =
+            legal_contingent_actions(&urza_info::observe(&state).unwrap(), &cards)[0].clone();
         apply_action(&mut state, &cards, cam_target).unwrap();
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
         apply_action(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8307:
-  &mut state,
-  &cards,
-  Action::ChooseCamEffect {
-      choice: CamEffectChoice::Decline,
-  },
+            &mut state,
+            &cards,
+            Action::ChooseCamEffect {
+                choice: CamEffectChoice::Decline,
+            },
         )
         .unwrap();
         let cam_canonical = urza_info::observe(&state)
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8315:
-  .unwrap()
-  .battlefield
-  .iter()
-  .find(|permanent| permanent.card == CAM)
-  .unwrap()
-  .canonical_id;
+            .unwrap()
+            .battlefield
+            .iter()
+            .find(|permanent| permanent.card == CAM)
+            .unwrap()
+            .canonical_id;
         apply_action(
-  &mut state,
-  &cards,
-  Action::ActivateGrantedKnackBounce {
-      source: ObjectId(1),
-      target: cam_canonical,
-  },
+            &mut state,
+            &cards,
+            Action::ActivateGrantedKnackBounce {
+                source: ObjectId(1),
+                target: cam_canonical,
+            },
         )
         .unwrap();
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8332:
         assert!(state.battlefield.get(ObjectId(1)).unwrap().tapped);
         // Resolve Cam LTB staging -> choose target -> resolve -> untap.
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
-        let action = legal_contingent_actions(&urza_info::observe(&state).unwrap(), &cards)[0].clone();
+        let action =
+            legal_contingent_actions(&urza_info::observe(&state).unwrap(), &cards)[0].clone();
         apply_action(&mut state, &cards, action).unwrap();
         apply_action(&mut state, &cards, Action::PassPriority).unwrap();
         apply_action(
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8339:
-  &mut state,
-  &cards,
-  Action::ChooseCamEffect {
-      choice: CamEffectChoice::Untap,
-  },
+            &mut state,
+            &cards,
+            Action::ChooseCamEffect {
+                choice: CamEffectChoice::Untap,
+            },
         )
         .unwrap();
         assert!(!state.battlefield.get(ObjectId(1)).unwrap().tapped);
Diff in /home/runner/work/urza-state-solver/urza-state-solver/rust/crates/urza-rules/src/lib.rs:8347:
     }
-
 }
 
