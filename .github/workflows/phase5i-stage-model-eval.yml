name: Phase 5I continuation-stage replications

on:
  push:
    branches:
      - phase5i-stage-model-eval
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Serial pilot superseded
        run: echo "Superseded by phase5i-stage-factorized-eval (4 independent hands per stage)."
